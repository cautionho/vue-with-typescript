class Filter {
	Html(s: string): string {
		return ''
	}

	Json(s: string): string {
		return ''
	}

	Repeat < T > (a1: T[], a2: T[], k: string): T[] {
		return [];
	}
}

export default new Filter();
